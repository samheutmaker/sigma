{
  "name": "sigma",
  "module": "src/app.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp index.html dist/ && cp styles.css dist/ && cp -r js dist/",
    "dev": "bun build src/app.ts --outdir dist --watch",
    "serve": "bun --bun serve.ts",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name sigma --commit-dirty=true"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.58.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
